# CLAUDE.md

このリポジトリで開発タスクを行う際の必読ドキュメント。
タスクに着手する前に、必ず下記の「マルチエージェント開発ポリシー」に従ってタスクを分割・委譲すること。

## プロジェクト概要

- スワイプ式RSSニュースリーダー。実装は `news/index.html` の**単一ファイル**（HTML/CSS/JSすべて内包、ビルドなし・依存なし）
- 状態は `localStorage`（キー `news-app-v1`）に保存
- 外部通信はCORSプロキシ経由のRSS取得と、Feedly検索API（`raceFirst` で全プロキシ並列レース）
- 学習ロジック: タイトルを文字bigram等に分解（`feats`）し、◎/×の出現率の対数オッズでマッチ度を算出（`scoreOf`/`pct`）

## マルチエージェント開発ポリシー

### 目的

トークン消費の効率化。ユーザーの依頼を受けたら、オーケストレーター（メイン）は
まず難易度を判定し、Agentツールで適切なモデルのサブエージェントに実装を委譲する。
統合・検証・コミット・報告はメインが行う。

### モデルの使い分け

| モデル | `model` 指定 | 担当する作業 |
|---|---|---|
| Fable | `fable` | 高難度: アーキテクチャ設計、数理・アルゴリズム（スコアリング、学習、ランキング）、原因不明のバグ調査、トレードオフ判断のある実装 |
| Sonnet | `sonnet` | 中難度: 仕様が明確な機能実装、UI/CSS変更、定型的なリファクタリング、ドキュメント作成 |
| Haiku | `haiku` | 低難度: コード検索・調査、文言修正、定数・設定値の変更、機械的な置換 |

### 難易度判定の目安

- **高（fable）**: 仕様が曖昧で解釈が必要 / 複数案のトレードオフ判断がある / 数理ロジックの設計・検証 / 影響範囲が横断的
- **中（sonnet）**: 仕様と完成形が明確で、実装パターンが定型的
- **低（haiku）**: 局所的・機械的で、判断がほぼ不要

迷ったら一段上のモデルに割り当てる（手戻りの方が高くつく）。
最終レビューや、サブエージェントの成果物を統合する判断が必要な場面は、
委譲せずオーケストレーター（メイン）が直接行う。

### 自動委譲ワークフロー（依頼を受けたときの標準フロー）

1. ユーザーの依頼を読み、単純作業か・仕様が明確か・数理/設計判断が要るかで難易度を判定する
2. 必要なら実装可能な単位のサブタスクに分解する
3. Agentツールの `model` パラメータで判定結果に応じたモデル（`fable`/`sonnet`/`haiku`）を明示して起動する
4. **調査・読み取り系タスクは並列実行してよい**（独立しているなら1メッセージで同時起動）
5. **編集タスクの並列実行は禁止**。実装が単一ファイルのため必ず競合する。
   編集は直列に委譲するか、サブエージェントには「変更案（diff形式）」だけ出させてメインが統合する
6. サブエージェントへのプロンプトは自己完結にする:
   対象ファイルパス、関連する関数名・行範囲、前提条件、期待する成果物の形式を明記する
7. サブエージェントの成果物を統合し、下記の検証手順を実施する
8. 検証が通ったらコミット・mainへマージ・プッシュする（サブエージェントにgit操作をさせない）
9. 完了したら、何を・なぜ変更したかをユーザーに日本語で報告する

### 検証手順（変更をコミットする前に必ず実施）

```bash
# <script> 部分を抽出して構文チェック
python3 -c "
import re
html=open('news/index.html').read()
m=re.search(r'<script>(.*)</script>',html,re.S)
open('/tmp/app.js','w').write(m.group(1))
"
node --check /tmp/app.js
```

- ロジック（`scoreOf` / `pct` / `rankResults` / `raceFirst` 等）を変更した場合は、
  該当関数をnodeスクリプトに切り出し、代表的な入力でシミュレーションして出力を確認する
- ネットワーク依存の関数は `fetch` をモックして検証する

### コミット規約

- コミットメッセージは日本語で、変更の理由（なぜ）を含める
- 1機能1コミット。検証が通ってからコミットする
